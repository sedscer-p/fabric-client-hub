# Data Storage

This folder stores meeting data for clients.

Structure:
- One folder per client (e.g., sarah-mitchell)
- Each client folder contains timestamped meeting folders
- Meeting folders contain: transcription.txt, summary.txt, actions.json, reports/
